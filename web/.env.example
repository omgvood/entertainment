# Скопируй в .env.local и подставь значения из Supabase:
# Project Settings → Data API → URL и publishable key (sb_publishable_...).
# Это публичные значения (передаются в браузер), но в репо их не коммитим.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
