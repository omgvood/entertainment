# Матчи спортклубов: подключить источники

Type: research
Скилл: `mattpocock-skills:research` — нужно оценить VK-группы клубов и SPA-сайты клубов (см. пометки в `seeds.yaml`) прежде чем решать, каким режимом их подключать
Status: backlog (волна 2)
Blocked by: —
Ветка: —

## Question

Раздел «Спорт (места)» покрывает постоянные площадки через 2ГИС, но матчи спортклубов не покрыты — в README помечено «в работе» (`README.md:68`): VK-группы клубов и сайты клубов на SPA размечены в `seeds.yaml`, но не подключены. Какие клубы есть, какой у них формат данных (VK-события / SPA с открытым JSON / SPA без API), и каким режимом извлечения (`vk_events`, `vk_posts`, `direct_api`, `playwright_listing`) их стоит подключать?

## Контекст

- `README.md:68` — статус «в работе», ссылка на пометки в `seeds.yaml`.
- `parser/config/seeds.yaml` — искать существующие пометки по спортклубам.
- Зависит от решения по [Судьба отключённых режимов: vk_events / kudago / playwright_listing](03-disabled-modes-decision.md) — если `vk_events` остаётся выключенным, клубы-сообщества VK придётся вести через `vk_posts` с более мягким префильтром, а не `vk_events`.

## Цифры

```sql
-- SQL: не прогнан
select count(*) from candidate_sources where city = 'perm' and category ilike '%спорт%';
```

## Кто зависит

| Место | Что делает | Меняется |
|---|---|---|
| `seeds.yaml` | Источники по городам | Да — новые источники клубов |
| `coverage_stats` | Снимок покрытия «Спорт» | Да — ожидается рост |

## Тесты, кодирующие старое поведение

| Файл:строка | Как выражено старое поведение |
|---|---|

## Готово когда

- Список клубов с оценкой формата данных и рекомендуемым режимом извлечения готов.
- Понятно, зависит ли подключение от решения по тикету 03 (vk_events).

## Answer

<Заполняется в конце сессии.>
