# Пробелы покрытия: стендап / дети / экскурсии / мастер-классы

Type: research
Скилл: `mattpocock-skills:research` — нужны факты о том, какие Telegram-каналы/VK-сообщества закрывают пробел по каждой категории, прежде чем заводить задачу на подключение
Status: open
Blocked by: —
Ветка: `research/discovery-coverage-gaps`

## Question

Четыре категории Перми помечены ⚠️ в README (`README.md:65`, `README.md:69-71`): стендап (нет категории в Timepad), дети/экскурсии/мастер-классы (Timepad покрывает частично). Какие конкретные источники (Telegram-каналы, VK-сообщества, сайты) закрыли бы каждый пробел, и что уже лежит необработанным в `candidate_sources`?

## Контекст

- `README.md:65`, `README.md:69-71` — таблица покрытия со статусами.
- `parser/src/parser/cli.py` — команды `discovery-health --city perm [--days N]` и `discover-sources`.
- `parser/src/parser/sources/candidate_sources.py` — Discovery новых источников.
- `parser/config/seeds.yaml` — текущий список источников по городам.

## Цифры

```sql
-- SQL: не прогнан
select status, count(*) from candidate_sources where city = 'perm' group by status;
```

```
# команда: не прогнана
python -m parser.cli discovery-health --city perm --days 30
```

`SQL/команда: прогнаны <дата>` — ставит сессия, которая их реально выполнила.

## Кто зависит

| Место | Что делает | Меняется |
|---|---|---|
| `seeds.yaml` | Список источников по городам | Да, если найдены кандидаты — добавляются новые `telegram_sources`/`vk_groups` |
| `coverage_stats` (таблица) | Ежедневный снимок покрытия по категориям | Нет, читается как критерий готовности |

## Тесты, кодирующие старое поведение

| Файл:строка | Как выражено старое поведение |
|---|---|
| — | Тикет не меняет код парсера, только собирает факты |

## Готово когда

- По каждой из четырёх категорий есть список конкретных источников-кандидатов (найденных руками или уже лежащих в `candidate_sources`) с оценкой, закрывают ли они пробел.
- Результат зафиксирован как отчёт (не план внедрения) — что именно найдено и что не найдено.

## Answer

<Заполняется в конце сессии.>

Локализация: `<команда>`, найдено N мест
Расхождения цифр: <или «нет»>
