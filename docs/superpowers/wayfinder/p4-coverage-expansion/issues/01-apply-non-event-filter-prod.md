# Фильтр не-событий: применить к боевой БД

Type: task
Скилл: не нужен — план уже написан (`docs/superpowers/plans/2026-09-18-non-event-filter.md`), реализация идёт по нему
Status: open
Blocked by: —
Ветка: `fix/apply-non-event-filter-prod`

## Question

Код фильтра не-событий смёржен в `master` (`README.md:125`), но накопленные в боевой БД записи почищены только вручную (15 строк: 11 по предиктору + 4 ручной дочистки). Нужно дождаться первого прогона крона новым кодом, выделить дельту — что попало в БД уже старым кодом до мерджа vs что могло проскочить новым — и применить `…_remove_non_events_social.sql` (или его актуальную версию) к остальному.

## Контекст

- `README.md:125` — описание трёх слоёв защиты и статус «применение к боевой БД — после первого прогона крона новым кодом».
- `docs/superpowers/plans/2026-09-18-non-event-filter.md` — план выделения дельты.
- `parser/src/parser/validator.py` (`is_non_event`), `parser/src/parser/pipeline.py` (`_safe_to_event_row`, счётчик `skipped_non_event` → `source_health`).
- Миграция `…_remove_non_events_social.sql` в `supabase/migrations/`.

## Цифры

```sql
-- SQL: не прогнан
select count(*) from events
where city = 'perm'
  and source like 'vk-%' or source like 'telegram-%'
  and created_at > '<дата первого прогона крона новым кодом>';
```

`SQL: прогнан <дата>` — ставит сессия, которая реально выполнила запрос и сверила дельту с `source_health.skipped_non_event`.

## Кто зависит

| Место | Что делает | Меняется |
|---|---|---|
| `source_health` (таблица) | Считает `skipped_non_event` за прогон | Нет, читается как источник цифр |
| `/perm/other` (фронт) | Показывает карточки типа `other` | Да — доля мусора должна упасть |

## Тесты, кодирующие старое поведение

| Файл:строка | Как выражено старое поведение |
|---|---|
| `parser/tests/test_non_event.py` | Уже проверяет `is_non_event`/guard на новом коде — не должен затрагиваться этим тикетом |

## Готово когда

- Первый прогон крона новым кодом состоялся, дельта (записи, попавшие в БД до мерджа) выделена запросом с датой прогона.
- Дельта проверена на признаки не-событий и очищена тем же критерием, что и миграция.
- Доля `/perm/other` карточек из `permactive` и других агрегаторов упала относительно базового значения ~27% (2026-09-18), зафиксированного в README.

## Answer

<Заполняется в конце сессии.>

Локализация: `<команда>`, найдено N мест
Расхождения цифр: <или «нет»>
