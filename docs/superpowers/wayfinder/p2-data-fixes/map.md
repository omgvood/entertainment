# Карта: данные и карточка под P2

Label: wayfinder:map
Status: активен
Эффорт: p2-data-fixes · Трекер: локальный markdown (`docs/superpowers/wayfinder/p2-data-fixes/`)
Заведена 2026-09-20 по итогам `docs/research/2026-09-19-p2-decision-helpers.md` и последующей проверки серых зон. Сведена к волне из трёх задач 2026-09-20.

## Destination

Каждая проблема, найденная исследованием P2, закрыта: механические — правкой в коде, спорные — записанным решением, серые зоны — фактом из источника. После этого P2 (чипы-сценарии, группы типов, быстрая карточка) можно планировать, не упираясь в качество данных.

**Эффорт закончен, когда:** цена различает «бесплатно» и «неизвестно» и в коде, и в данных; про время события известно, что честно показывать; группы типов и их формат в URL решены; проверки серых зон дали факты, а не догадки. Тикеты вне текущей волны к этому моменту либо закрыты, либо осознанно выброшены.

## Текущая волна

Одновременно открыто не больше трёх задач: одна блокирующая плюс дешёвые параллельные. Остальное ждёт в бэклоге и детализируется, когда до него дойдёт очередь.

**Волна 1 закрыта (2026-09-20).** Все три тикета — `## Answer`:

| Тикет | Тип | Статус |
|---|---|---|
| [Фильтр не-событий: применить миграцию и запушить master](issues/03-apply-non-event-migration.md) | task | ✅ resolved — миграция применена, master запушен 22 коммита, Vercel success |
| [Цена: `0 ₽` больше не «бесплатно»](issues/01-price-zero-rub.md) | task | ✅ resolved — правка `FREE_RE`, 66 событий переквалифицированы free→unknown |
| [Час города: helper текущего времени](issues/02-city-clock-helper.md) | task | ✅ closed — `getCityNowMinutes` в `dateUtil.ts`, 96 тестов зелёные |

Дальше — новое сведение: посмотреть, что изменилось, выбрать следующие три задачи из бэклога.

## Текущая волна (волна 2)

Открыта 2026-09-20, **закрыта 2026-09-22** — все три тикета с `## Answer`:

| Тикет | Тип | Статус | Зачем сейчас |
|---|---|---|---|
| [Проверка «бесплатно» по источникам](issues/10-verify-free-price.md) | research | ✅ resolved — `## Answer` есть | **Блокирующая** — держит тикет 08 (цена в схеме). Нужны факты: вправду ли события, помеченные «бесплатно», бесплатны. Результат влияет на модель данных. Параллельная работа с 13 и 04 |
| [Карточка без картинки: как она выглядит](issues/13-card-without-image.md) | prototype | ✅ closed — `## Answer` есть | 43% событий без картинок. Прототип дизайна: скрывать, показывать плоской строкой или оставить как есть. Результат нужен для 07 |
| [Пустой город](issues/04-empty-city-state.md) | task | ✅ closed — `## Answer` есть | Простая правка: UI пустого города. Тикет 03 освободил — Сочи будет пуст 21.09. Готова сейчас |

**Волна 3 закрыта (2026-09-22).** Все три тикета — `## Answer`, ветки смержены в `master` (PR #21, #23, #25):

| Тикет | Тип | Статус | Зачем сейчас |
|---|---|---|---|
| [endDate филармонии: шаблон или факт](issues/11-filarmonia-endtime.md) | research | ✅ resolved — `## Answer` есть | Блокирующая — держит тикет 06. Разблокирована тикетом 10 не была, шла независимо |
| [Цена в схеме: бесплатно против неизвестно](issues/08-price-unknown-in-schema.md) | grilling | ✅ resolved — `## Answer` есть | Разблокирован тикетом 10 (resolved волна 2), дёшев |
| [category и district: зафиксировать непригодность](issues/17-category-district-dead-fields.md) | task | ✅ closed — `## Answer` есть | Мелкая документационная правка, ни от чего не зависит |

**Волна 4 закрыта (2026-09-22).** Все три тикета — `## Answer`:

| Тикет | Тип | Статус | Зачем сейчас |
|---|---|---|---|
| [Политика «идёт сейчас» и прошедших сегодня](issues/06-now-and-past-policy.md) | grilling | ✅ resolved — `## Answer` есть | Разблокирована тикетом 11 (resolved волна 3). Держит тикет 07 |
| [Точность типов у direct_api](issues/12-direct-api-type-accuracy.md) | research | ✅ resolved — `## Answer` есть | **Блокирующая** — держит тикет 09 |
| [Дубли, которые серия не склеивает](issues/14-unmerged-duplicates.md) | grilling | ✅ resolved — `## Answer` есть | Независима и дёшева |

**Волна 5 закрыта (2026-09-22).** Все три тикета — `## Answer`:

| Тикет | Тип | Статус | Зачем сейчас |
|---|---|---|---|
| [Карточка быстрого просмотра](issues/07-quick-scan-card.md) | task | ✅ resolved — код смёржен, PR #31 | Разблокирован тикетами 06 и 13 (оба закрыты). Решения по политике «идёт сейчас» и картинке готовы к реализации |
| [Теги: определения, авто-теги, слияние](issues/09-tag-quality.md) | grilling | ✅ resolved — `## Answer` есть | Разблокирован тикетом 12 (resolved волна 4) |
| [Многодневные события: дата начала и конца](issues/15-multiday-events.md) | grilling | ✅ resolved — `## Answer` есть | Независим, дёшев (grilling) |

## Текущая волна (волна 6)

Открыта 2026-09-22, подтверждена пользователем. Последняя волна эффорта — после неё бэклог пуст.

| Тикет | Тип | Статус | Зачем сейчас |
|---|---|---|---|
| [Группы типов и формат URL](issues/05-type-groups-url.md) | grilling | ✅ resolved — `## Answer` есть | Разблокирован (Blocked by: —). Самый спорный и дорогой — решение меняет URL-состояние P1 |
| [Полнота афиши: чего мы не находим](issues/16-afisha-recall.md) | research | открыт | Разблокирован (Blocked by: —). Независим, полевая проверка внешних афиш |

Дальше — после закрытия волны бэклог этого эффорта пуст.

## Decisions so far

<!-- одна строка на закрытый тикет -->

- **01 Цена `0 ₽`** (2026-09-20): у источника (сайт Пермской галереи) все три проверенных события платные — `0 ₽` значит «цену не знаем», правка правомерна. Ветки с нулём убраны из `FREE_RE`; 66 будущих событий Перми сменили `free` → `unknown` (free 193 → 127, unknown 84 → 150, known 95), цифры БД сошлись со снимком тикета один в один. Две дыры в тикете: старое поведение держал ещё и `search.test.ts`, а SQL в разделе «Цифры» не выполняется без `price_text` в CTE `k`. `README.md:1917` остался со старыми цифрами — вне scope.
- **02 Час города** (2026-09-20): в `web/lib/dateUtil.ts` добавлен `getCityNowMinutes(city)` — минуты от местной полуночи через `Intl.DateTimeFormat` с `hourCycle: "h23"` (при `hour12: false` полночь приходит как `24:00`). Развилка «`HH:MM` или минуты» решена в пользу минут: оба потребителя (06, 07) считают разницу, а «началось в 15:00» берёт время из данных события. Локализация показала, что потребителей «часов города» на `master` нет вообще (`getHours()` в `web/` не встречается), таймзоны заданы в одном месте — правка чисто аддитивная. `npx vitest run` — 96 тестов зелёные, `tsc --noEmit` чистый.
- **03 Фильтр не-событий** (2026-09-20): миграция применена (удалено 14 строк, `other` в Перми 37 → 26, будущих 386 → 374, хвост 2027+ 4 → 1), `master` запушен (22 коммита), Vercel `success`, лента дат на живом сайте. Сочи остался с 2 сегодняшними событиями, с 21.09 пуст — это делает видимым тикет 04.
- **10 Проверка «бесплатно» по источникам** (2026-09-21): выборка 28 событий с `price_text`≈«бесплатно» — только 7 (25%) подтвердились на источнике, 13 (46%) явно платные (источник продаёт билеты, слова «бесплатно» на источнике нет вообще), 6 (21%) источник не говорит о цене, 1 не событие, 1 `source_url` не пермалинк. Системная ошибка: `generic:teatr-teatr.com` — 3/3 проверенных помечены free, все явно платные. Отдельно 4 из 6 уникальных `source_url` с `price_text`=«0 ₽» (Пермская галерея) подтвердили вывод тикета 01: «0 ₽» = цену не извлекли, у источника она есть. Вывод для тикета 08: `price_text` как прямой признак «бесплатно» не годится, постфильтр «рядом со словом билет» не решит — ошибка не в соседних словах, а в немотивированной галлюцинации LLM-экстрактора.
- **08 Цена в схеме** (2026-09-22): новая enum-колонка `price_kind` (`known`/`free`/`unknown`, `NOT NULL DEFAULT 'unknown'`) вместо булева — LLM получает новое обязательное поле наравне с `price_min/max/text` и пишет `free` только при дословном «бесплатно»/«вход свободный» в тексте, иначе `unknown`; регексп-эвристика `price.ts::priceKind()` над `price_text` уходит, читает колонку напрямую. Бэкфилл — `unknown` для всех старых строк, не старым регекспом (перенёс бы галлюцинацию тикета 10 в схему). Решение опиралось на тикет 10: `price_text` как признак «бесплатно» ошибается в 46% случаев. Код и миграция — отдельным тикетом, вне этого. Не проверено: надёжность `free` у структурных источников `jsonld`/`timepad` (цена `0` в `offers`).
- **04 Пустой город** (2026-09-22): `EmptyState` получил `cityEmpty = suggestion === null && liveEvents.length === 0` — при полностью пустом городе текст меняется на «В городе пока нет событий» без совета выбрать другой день; кнопка «Ближайшее» и так не показывалась (`suggestion === null`). Проверено вживую на `/sochi` (0 событий). `/perm` для проверки «дата пуста, город нет» не поднялся — `ECONNRESET` к Supabase в песочнице, к правке не относится. Побочный инцидент: другая параллельная сессия в основном каталоге `C:\Python\entertainment` откатила первую попытку правки (`git checkout master` + `reset` между командами) — работа перенесена в отдельный worktree `C:\Python\entertainment-fix-empty-city-state`, ветка `fix/empty-city-state`, коммит `c04c331`, смёржено в `master` PR #20.
- **11 endDate филармонии** (2026-09-22): шаблон, не факт. `generic:filarmonia.online` читает только листинг `/afisha`, у которого JSON-LD ставит `endDate = startDate + 180 мин` для всех событий без исключения (без поля `duration`). Страница самого события даёт другой, реальный JSON-LD с явным `duration` — доказано на одном и том же событии («Экскурсия «Органное закулисье»», 24.09): листинг даёт 22:00 (+3ч), страница события — `duration PT1H30M` → 20:30. Ещё два примера с той же страницы события: «Мультконцерт» `PT2H`, «Вечер при свечах» `PT1H30M` — ни один не совпадает с шаблонными +3ч. Рекомендация для тикета 06: `time_end` этого источника с листинга не использовать (`None`), либо дорогой вариант — отдельный fetch страницы события. `teatr-teatr.com` и `permm` вопрос не касается: у первого JSON-LD `Event` нет вообще (LLM-экстракция, `time_end` уже `NULL` у всех 14), второй (`permm.py`) `time_start`/`time_end` не читает и не пишет в принципе.
- **13 Карточка без картинки** (2026-09-22): выбран вариант **D** после того как пользователь отклонил B («выглядит пусто») и C («скрываем, а не решаем проблему»). D оставляет слот картинки и порядок сетки как сейчас, но на плейсхолдере поверх градиента показывает цену (`event.priceText`) вместо голой эмодзи. Альтернатива «фото площадки вместо плейсхолдера» проверена в БД и отклонена фактом: 0 из 132 будущих пермских событий без картинки имеют площадку с фото, в `venues.image_url` нет данных вообще (0/27 Пермь, 0/40 Сочи) — отдельная задача на источники фото площадок. Прототип (ветка `proto/card-without-image`, не мерджится) — источник для реализации в тикете 07, детали в `## Answer` тикета 13. Побочный инцидент: общий каталог `C:\Python\entertainment` дважды переключался параллельными сессиями на другие ветки во время работы — прототип пришлось перенести в отдельный worktree `C:\Python\entertainment-proto-card-without-image`.
- **06 Политика «идёт сейчас»** (2026-09-22): «идёт сейчас» показываем только при надёжном `time_end` в `[start,end]` — это ~3% событий (10/359: 41 с `time_end` минус 31 филармония с шаблонным `+3ч`, тикет 11 признал его не фактом); остальным — относительная метка от `time_start` («началось в HH:MM»), без бейджа. Порог «начавшееся сегодня» — 3 ч от `time_start`: до порога карточка на месте с меткой, после — уводится в конец списка дня, но не скрывается (нет данных, что событие правда закончилось). Счётчики чипов дат считают «доступное»: событие входит в число, если `time_start` неизвестен или порог не прошёл; исключённые из счёта остаются в ленте. Приоритет — честность числа, выбран пользователем явно. Реализация — тикет 07.
- **12 Точность типов у direct_api** (2026-09-22): чинить надо маппер, а не карту тегов, и только у двух источников. `permopera`: у театра в выборке 0/9 настоящих «концертов» — жанр на странице всегда другой (опера, экскурсия, музыкальный спектакль-прогулка «другие события»), потому что `event_type` берётся не из события, а хардкодится одной строкой в `parser/config/seeds.yaml` для всего источника. `permm`: ошибка точнее контекста тикета — не весь маппер, а конкретно фид `_EVENTS_URL` (`/json/events/sub/home`, по докстрингу сам «экскурсии, лекции, фестивали»), который получает тот же `event_type=exhibition`, что и честный `_EXHIBITIONS_URL` — 2/2 проверенных события из этого фида не выставки, доля по выборке ~22% (2/9). `timepad` и `quizplease` не трогать: категория Timepad совпала с сайтом в 3/3 проверок, «непротегированность» 7/8 событий — вопрос покрытия `TYPE_DEFAULT_TAGS`, не бага типа. Разблокирован тикет 09.
- **17 category/district мёртвые поля** (2026-09-22): README, п. 14 — покрытие `district` (3/359) и `category` (35/359, свободный текст без словаря) записано там же, плюс отличие от `_map_category` Timepad (маппинг в `EventType`, другое поле). Комментарий на `category?: string` в `web/lib/types.ts`. Локализация грепом подтвердила: блок «Район» в `FilterBar.tsx` уже удалён P1 (совпадает с описанием в тикете), кода `category`/`district` событий больше нигде не касается. Только документация, ветка `fix/dead-fields-note`, смёржено в `master` PR #21.
- **09 Теги: определения, авто-теги, слияние** (2026-09-22): «для пары» (40–60% точность) получает определение в промпте («камерное/романтическое на двоих, не для компании и не для семьи»), остальные 10 тегов не трогаем — измеренной проблемы нет только у неё и у «для детей» (≈90%). `TYPE_DEFAULT_TAGS` подставляется только `direct_api`-источникам (`SOCIAL_SOURCE_PREFIXES`-проверка, как в `is_spurious_always`), а не всем с пустыми тегами — правит расхождение кода `validator.py:82-83` с его же комментарием и убирает 9/13 ошибок «вечером». При слиянии дублей конфликтующая пара тегов (`вечером`/`днём`, `в помещении`/`на улице`) решается в пользу тега строки-победителя по `priority`, не объединяется. `TAGS_VERSION` поднимается, перепарсинг обязателен. Код — отдельным тикетом.
- **14 Дубли, которые серия не склеивает** (2026-09-22): 19 живых пар `dedup_candidates` делятся по `title_score` на три группы — 6 с `title_score=1.000` (настоящие дубли, площадка названа по-разному между источниками), 8 «Балетный квартал» (ложные срабатывания, `title_score` 0.754–0.842, никогда не 1.000, венue дословно совпадает), 5 прочих настоящих дублей (0.771–0.833, тот же диапазон, что и «Балетный квартал» — score их не различает). Решение: в `fuzzy.py::score_pair` не занижать через `_venue_factor` пары с `title_score == 1.0` — закрывает 6/19 без риска задеть «Балетный квартал». Остальные 13 разрешателем не чинить (дороже пользы для ~5 реальных дублей в снапшоте), мириться на фронте — визуальный шум, не искажение фактов. Накопленные пары руками не трогать, `dedup_candidates` пересчитывается каждым прогоном (TTL 1 день). Код — отдельным тикетом.

- **15 Многодневные события** (2026-09-22): не заводить `date_end` — выставки уже несут многодневность строкой-на-каждый-день, а `web/lib/series.ts::otherDates` уже группирует их по title+venueName и считает «ещё N дат»; для фестивалей достаточно того же паттерна, а не новой колонки. Промпт получает второе поле `date_end` (по умолчанию = `date_start`), парсер разворачивает диапазон в N строк `EventRow`. Граница разворачивания — 21 день (все восемь примеров тикета, включая 16-дневный цирк, укладываются); длиннее — сигнал ошибки LLM (спутала фестиваль с сезоном), остаётся одна строка на `date_start` с логом-предупреждением. Бэкфилл не нужен — уже записанные фестивали дозаполнятся следующим ежедневным прогоном через upsert по новому slug каждого дня. Тикет 06 («идёт сейчас») получает многодневные события как обычный набор строк по датам без отдельного случая. Код — отдельным тикетом.

- **05 Группы типов и формат URL** (2026-09-22): 7 групп чипов + отдельный чип-аудитория «Детям» (только тип `kids`, без объединения с тегом «для детей» — осознанное упрощение для P2), `other`/`food` (18 событий) убраны из ряда чипов совсем, без переноса и без своей кнопки, но остаются в общей ленте. Клик по группе раскрывается в список конкретных типов в `?type=` (`urlState.ts` не меняется, старые ссылки P1 продолжают работать) — не новый параметр `group=`. Карта групп — только фронт (`web/lib/`), `taxonomy.py` не трогаем. Цифры пересчитаны от живого Supabase на 2026-09-22 (317 будущих событий Перми), а не от снимка тикета на 19.09. Код — отдельным тикетом.

## Бэклог вне волны

Тикеты написаны, но не в работе: `Status: backlog`. Брать их, не закрыв волну, — ровно та ошибка, из-за которой работа встала 2026-09-20. Перед взятием перечитать: цифры в них — снимок на 19–20 сентября.

**Про типы и URL**
- [Группы типов и формат URL](issues/05-type-groups-url.md) — grilling; 22 типа в 6–8 групп, стык с URL-состоянием P1.

**Проверки серых зон**
- [Полнота афиши: чего мы не находим](issues/16-afisha-recall.md) — research; никого не держит.

## Плановый порядок волн (не детализировать заранее)

Черновая последовательность для 11 тикетов бэклога, собранная из зависимостей, уже записанных в разделе выше (кто кого держит). Это **порядок, не содержание**: цифры, файлы, ветки и критерии готовности каждый тикет получает только на сведении перед своей волной — они протухают за сутки (см. `Decisions so far`, тикет 01).

| Волна | Тикеты | Почему в этом порядке |
|---|---|---|
| 3 ✅ закрыта 2026-09-22 | 11, 08, 17 | 11 — блокирующая (research, держит 06→07); 08 разблокирован тикетом 10 (resolved) и дёшев; 17 — мелкая документационная правка, ни от чего не зависит |
| 4 ✅ закрыта 2026-09-22 | 06, 12, 14 | 06 разблокирован тикетом 11; 12 — блокирующая (research, держит 09); 14 независим и дёшев (grilling по дублям) |
| 5 ✅ закрыта 2026-09-22 | 07, 09, 15 | 07 разблокирован тикетами 06 и 13; 09 разблокирован тикетом 12; 15 независим (grilling по многодневным событиям) |
| 6 (следующая) | 05, 16 | 05 — стык с URL-состоянием P1, самый спорный и дорогой, поэтому последний; 16 никого не держит и не блокируется — можно в любой момент, оставлен на конец как наименее срочный |

Каждая волна всё равно проходит обычное сведение перед открытием: порядок здесь — гипотеза, а не готовое решение, её может пересмотреть то, что узнали на предыдущей волне. Волна 6 — последняя в этом эффорте, дальше бэклог пуст.

## Notes

- **Домен:** агрегатор афиши Перми и Сочи. Парсер — `parser/`, фронт — Next.js 16 SSG в `web/`, хранилище — Supabase Postgres (таблица `events`), сайт пересобирается раз в сутки, фильтрация на клиенте.
- **Волна.** Открыто не больше трёх тикетов. Детализируется ровно одна волна вперёд, остальное живёт строкой в бэклоге или в тумане. Разбор, почему это правило появилось, — в `docs/agents/process-journal.md`.
- **Сверка состояния перед работой:** `git log --oneline origin/master..master`, что влито, что на проде, что в БД. Карта изначально строилась от устаревшего `origin/master`, и часть тикетов описывала уже сделанную работу.
- **Отклонение от дефолта wayfinder (согласовано 2026-09-20):** карта несёт исполнение, а не только решения. Чем заканчивается тикет — определяет его тип:
  - `task` — правка в коде с проверкой (тесты Vitest для `web/lib`, pytest для парсера) или выполненная ручная работа;
  - `grilling` — записанное решение; код по нему заводится отдельным тикетом уже с готовым ответом;
  - `research` — факт, добытый из первоисточника;
  - `prototype` — черновой артефакт, на который можно посмотреть и отреагировать.
- **Параллельность:** каждый тикет — своя ветка `fix/<slug>` или `research/<slug>` от `master`, свой PR. Перед `git add` отдельным шагом `git branch --show-current`. Force-push запрещён, пуш — только с согласия пользователя.
- **P0/P1 уже в `master`** (коммиты `b6c776c`…`e53de3e`): лента дат, серии, чипы типов, состояние в URL, «Другие даты». Ветки тикетов брать от `master`, а не от `origin/master`. `origin/master` отстаёт, поэтому живой сайт ещё без P1 — это чинит тикет 03.
- **Скилл указан в каждом тикете** строкой `Скилл:` — либо имя, либо «не нужен» с причиной. Выводить его из типа не надо. Правило: скилл берётся, если меняет способ работы (TDD заставляет писать падающий тест первым, research — идти в первоисточник, brainstorming — показать варианты с ценой ошибки, prototype — сделать артефакт вместо описания). Если работа механическая, а критерий готовности уже в тикете, скилл не нужен.
- **БД:** только чтение через Supabase MCP. Записи и миграции — отдельным согласованием.
- Цифры в тикетах — снимок на 2026-09-20: Пермь, 359 будущих событий.

## Not yet specified

- Как считать числа у чипов даты и сценариев после того, как появится политика прошедших событий. Зависит от тикета 06.
- Нужен ли разрешатель `dedup_candidates` (LLM или человек) и какой порог у fuzzy-скорера. Зависит от тикета 14.
- Нужно ли отдельное поле длительности или часов работы площадки, чтобы выставки честно отвечали на «открыто сейчас». Зависит от тикетов 11 и 15.
- Форма чипов-сценариев в вёрстке (отдельный ряд, внутри ленты дат, в поповере фильтров). Зависит от тикета 05.

## Out of scope

- **Источники Сочи.** Прогон 18.09: Timepad — `HTTP 403 — проверь токен/ключ API`, каждый 2ГИС — по 10 ошибок, VK принёс 3 события, Telegram 1. Город пуст на источниках, а не в БД. Это работа с ключами и подбором групп, отдельным эффортом.
- **Мобильная вёрстка** плотной карточки и ленты — роадмап README, пункт 19.
- **Сам P2** (чипы-сценариев, группы типов в UI, новая карточка как фича) и **P0/P1** — эта карта их разблокирует, но не реализует.
