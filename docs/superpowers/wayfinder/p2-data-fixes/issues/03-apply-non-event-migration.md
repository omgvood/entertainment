# Фильтр не-событий: применить миграцию и запушить master

Type: task
Скилл: не нужен — кода нет, вся ценность в согласиях и в проверке из раздела «Готово когда»
Status: resolved
Blocked by: —
Ветка: работа в `master` (пуш) плюс запись в БД

## Question

Код фильтра не-событий уже в локальном `master` (`1eaab55`, `b043ef4`, `91e68b2`, `0ed2466`, `7c43fd9`), но миграция `20260918000001_remove_non_events_social.sql` к БД не применена, а сам `master` не запушен. Из-за первого в выдаче висят новости и реклама, из-за второго живой сайт собирается со старого `origin/master`.

## Контекст

- Проверено 2026-09-20: 13 строк из списка миграции лежат в `events`, 12 из них — будущие события Перми с `type='other'`.
- После применения: `other` в Перми 37 → 25, всего будущих 359 → 347, хвост 2027–2028 почти исчезает, у Сочи не остаётся ни одного будущего события.
- `origin/master` отстаёт на 16 коммитов: кроме фильтра не-событий там вся реализация P1 (лента дат, серии, чипы, URL, «Другие даты»). Живой сайт https://entertainment-eta.vercel.app/perm всего этого ещё не показывает.
- Фильтр ловит не всё: «Проверка школьников на склонность к суицидальному поведению» (01.10) в списке нет и предикатом не ловится.
- Запись в БД и пуш — необратимые действия, каждое с отдельного согласия пользователя.

## Готово когда

- Миграция применена, проверка из её хвоста возвращает 0 строк.
- `master` запушен, сборка Vercel прошла, на живом сайте видна лента дат.
- В ответ тикета записано: сколько строк удалено, каким стал `other` и общее число будущих событий.

## Answer

Закрыт 2026-09-20. Оба необратимых действия выполнены с отдельного согласия: сначала запись в БД, потом пуш.

**Миграция.** Применена через Supabase MCP `apply_migration`, имя `remove_non_events_social` — тем же способом, каким применялись предыдущие миграции проекта (история хранит MCP-таймстемпы, не версии из имён файлов). Удалено **14 строк**, таблица `events` 523 → 509. Контрольный `SELECT` по списку id вернул **0 строк**.

**Цифры до и после** (замер 2026-09-20, будущие = `date >= CURRENT_DATE`):

| | До | После |
|---|---|---|
| `other` в Перми | 37 | **26** |
| Будущих всего | 386 | **374** |
| Пермь | 383 | 372 |
| Сочи | 3 | **2** |
| Хвост 2027+ | 4 | 1 |

**Расхождения с контекстом тикета** (снимок 19–20.09 устарел за сутки — открытый вопрос 8 журнала):
- В списке миграции 15 id, в БД на момент применения лежало **14**, а не 13: `perm-maksim-iz-krasnokamska-v-teleproekte-zhduli-2026-09-17` уже отсутствовал.
- Будущих было **386**, а не 359: парсер добирал события после замера.
- `other` в Перми стал **26**, а не 25: две строки «Бесплатные парковки» (18 и 19.09) к моменту применения уже стали прошедшими, так что из будущих удалились 11 пермских, а не 12.
- «У Сочи не остаётся ни одного будущего события» — **неверно**: остались два сегодняшних (Арбенина, open-air «Где мой 2008?»). Пустым город станет 21.09 — на сайте уже видно «Завтра 0». Это состояние закрывает тикет 04.

**Пуш.** `b27c2b9..93401a7`, 22 коммита (в тикете было 16 — добавились docs-коммиты карты и разведения `docs/`). Отдельным коммитом ушла лежавшая в рабочем дереве правка `Status:` в шапке карты и неотслеживаемый план `docs/superpowers/plans/2026-09-18-non-event-filter.md`. Сборка Vercel — `success`.

**Проверка живого сайта** https://entertainment-eta.vercel.app/perm: лента дат («Сегодня 63 · Завтра 9 · Выходные 63 · … · Все даты 310»), чипы типов, серии («ещё 1 дата до 21 сентября») — P1 виден. Ни один из 9 маркеров удалённых не-событий («Бесплатные парковки», «Розыгрыш металлической двери», «Open call на 13 сезон», «Гражданин Мексики», «Отслеживание цен», «пассажирское судно», «Голосование за номинантов», «Капитальный ремонт путепровода», «Пиццбург») не встречается в HTML страницы — проверен весь набор, а не видимая часть.

**Хвост.** 310 карточек на сайте против 372 будущих строк Перми в БД — это группировка серий, не потеря данных. «Проверка школьников на склонность к суицидальному поведению» (01.10) в выдаче осталась: предикат её не ловит, в список миграции она не входила.
