# Макет светлой палитры по спеку

Type: prototype
Скилл: `mattpocock-skills:prototype` — пользователь хочет увидеть палитру глазами до плана и кода; текстовый спек уже согласован
Status: open
Blocked by: —
Ветка: `proto/light-palette-mockup` (не мерджится)

## Question

Спек светлой палитры согласован в чате текстом (токены, пастель категорий, заглушки). Пользователь просил сначала сделать по нему макет: так ли выглядят главная, страница события и площадки на реальных данных, как задумано, и нужны ли правки значений до того, как писать план.

Отдельно проверить риск, записанный в спеке: розовая группа категорий (`#FDECF2` / `#A3154A`) близка к акценту `#D81B60`, и бейдж «Стендап» может читаться как кликабельный.

## Контекст

- Спек: [2026-09-22-light-palette-design.md](../../../specs/2026-09-22-light-palette-design.md) — палитра, категории, картинки, список файлов с номерами строк.
- Исследование и сравнение с Яндекс Афишей, KudaGo, Timepad, Luma, DICE, Time Out — в диалоге 2026-09-22, выводы перенесены в раздел спека «Зачем».
- Предложение, как делать макет (решается в сессии):
  - **на реальных данных** — на ветке `proto/…` заменить только токены в `web/app/globals.css`, таблицы в `web/lib/event-styles.ts` и `web/lib/venue-styles.ts` и градиент в `web/components/Header.tsx`, без логики картинок. Смотреть на превью Vercel (Preview env настроен 2026-06-26, см. память `vercel-preview-env`). Плюс: видно на живых карточках. Минус: превью строится несколько минут;
  - **статичный HTML** в `prototype/` — быстрее, но на выдуманных данных.
- `prototype/redesign/` — прошлая светлая итерация от 2026-08-17 (акцент `#6c4cff`), почему её сменили на неон — не записано. Можно спросить пользователя.

## Цифры

Контраст всех пар посчитан скриптом по формуле WCAG 2.x, значения в спеке. На макете их надо перемерить на отрисованной странице — прозрачности и наложения могут дать другие числа.

## Кто зависит

| Место | Что делает | Меняется |
|---|---|---|
| `web/app/globals.css` | токены палитры | на proto-ветке |
| `web/lib/event-styles.ts`, `web/lib/venue-styles.ts` | цвета бейджей и заглушек | на proto-ветке |
| `web/components/Header.tsx` | градиент логотипа | на proto-ветке |
| спек `2026-09-22-light-palette-design.md` | источник значений для плана | да — правки по итогам макета |

## Тесты, кодирующие старое поведение

| Файл:строка | Как выражено старое поведение |
|---|---|
| — | стили тестами не покрыты (поиск по `gradient`, `emoji` в `web/lib/*.test.ts` 2026-09-22 — 0 совпадений) |

## Готово когда

- Пользователь посмотрел скриншоты `/perm`, страницы события с фото и без, `/perm/venues`, страницы площадки на ширине 1280 и 375 и сказал «ок» или дал правки.
- Правки внесены в спек (разделы «Палитра» и «Категории»), строка `Status:` спека → «макет согласован».
- Ссылка на превью или файл макета записана в `## Answer`; proto-ветка не мерджится.
- Выполнено обновление файла map.md

## Answer

<Заполняется в конце сессии.>

Локализация: `<команда>`, найдено N мест
Расхождения цифр: <или «нет»>
