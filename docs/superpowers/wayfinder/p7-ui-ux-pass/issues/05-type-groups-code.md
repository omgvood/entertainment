# Группы типов: код по решению p2 не написан

Type: task
Скилл: `superpowers:test-driven-development` — у фильтров и URL-состояния есть тесты (`filters.test.ts`, `urlState.test.ts`), новое поведение групп фиксируется тестом первым
Status: backlog (волна 2)
Blocked by: —
Ветка: `feat/type-groups`

## Question

Решение «Группы типов и формат URL» принято 2026-09-22 (карта p2, тикет 05) и закончилось фразой «Код — отдельным тикетом», но тикет не был заведён, а код не написан. На живом сайте 2026-09-22 ряд категорий прежний: «Выставка», «Концерт», «Спектакль», «Детям», «Ещё 14» — в выпадающем списке первым идёт «Другое», есть «Еда», хотя по решению `other` и `food` из ряда чипов убираются.

## Контекст

- Решение: `docs/superpowers/wayfinder/p2-data-fixes/issues/05-type-groups-url.md`, `## Answer`. Кратко: 7 групп и отдельный чип «Детям» (только тип `kids`); `other`/`food` не показываются в ряду чипов, но остаются в ленте; клик по группе раскрывается в список типов в `?type=`, `urlState.ts` не меняется, старые ссылки P1 работают; карта групп — только фронт (`web/lib/`), `taxonomy.py` не трогать.
- Поиск 2026-09-22: `TYPE_GROUPS` / `typeGroups` в `web/lib` и `web/components` — 0 совпадений.
- Чипы и «Ещё N»: `web/components/FilterBar.tsx`.
- «Детям» отдельной кнопкой — осознанное решение p2/05. В первом UX-разборе 2026-09-22 это было названо «смешением аудитории с жанрами» — повторно не открывается.
- **Пересечение:** тикет 07 правит `FilterBar.tsx` (цвета и тени). Этот тикет идёт раньше (волна 2).

## Цифры

На 2026-09-22 — 317 будущих событий Перми (снимок из `## Answer` тикета p2/05, пересчитан там от живого Supabase). Раскладку по группам перепроверить перед правкой.

```sql
-- SQL: не прогнан
select type, count(*) from events
where city = 'perm' and date >= to_char(current_date, 'YYYY-MM-DD')
group by 1 order by 2 desc;
```

## Кто зависит

| Место | Что делает | Меняется |
|---|---|---|
| `web/lib/` (новая карта групп) | группа → список `EventType` | да, новое |
| `web/components/FilterBar.tsx` | ряд чипов и «Ещё N» | да |
| `web/lib/urlState.ts` | `?type=` | нет (по решению) |
| `web/lib/filters.ts` | отбор по `type` | проверить — вероятно, нет |

## Тесты, кодирующие старое поведение

| Файл:строка | Как выражено старое поведение |
|---|---|
| `web/lib/urlState.test.ts` | формат `?type=` — должен остаться зелёным без правок |
| `web/lib/filters.test.ts` | отбор по типам — проверить, что не держит список чипов литералом |

## Готово когда

- В ряду: 7 групп + «Детям»; `other` и `food` в ряду нет, в ленте их события есть.
- Клик по группе даёт `?type=a,b,c`; старая ссылка `?type=concert` открывается как раньше — тест.
- `npx vitest run`, `npm run build`, `npm run lint` проходят; проверено на превью.
- Выполнено обновление файла map.md

## Answer

<Заполняется в конце сессии.>

Локализация: `<команда>`, найдено N мест
Расхождения цифр: <или «нет»>
