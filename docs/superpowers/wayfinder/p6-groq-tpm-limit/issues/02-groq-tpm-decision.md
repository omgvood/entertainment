# Groq TPM-лимит: стоит ли что-то менять

Type: grilling
Скилл: `grilling` + `domain-modeling` — решение продукта на основе цифр из тикета 01, не факт и не код
Status: open
Blocked by: [Groq TPM-лимит: как часто и насколько дорого фолбэк на Gemini](01-groq-fallback-frequency.md)
Ветка: —

## Question

По цифрам из тикета 01 — стоит ли что-то менять в цепочке LLM-провайдеров (снизить `POST_BATCH_MAX_CHARS`/`POST_BATCH_SIZE` под Groq, поменять порядок `LLM_FALLBACK_PROVIDERS`, добавить платный tier Groq, или оставить как есть, раз фолбэк и так работает)? Вопрос открыт только если тикет 01 покажет, что отток создаёт реальный риск (частое исчерпание дневной квоты Gemini, деградация полноты покрытия) — иначе закрывается как «не проблема».

## Контекст

- `README.md:127`, `README.md:129` — текущее поведение и уже принятое решение «параметр под Groq не подгоняем».
- `config.py` — `POST_BATCH_SIZE`, `POST_BATCH_MAX_CHARS`, `LLM_FALLBACK_PROVIDERS`, `LLM_RETRY_ATTEMPTS`.
- Результат тикета 01 — обязательная вводная перед этим тикетом.

## Цифры

Берутся из `## Answer` тикета 01, не измеряются заново.

## Кто зависит

| Место | Что делает | Меняется |
|---|---|---|
| `config.py` (`POST_BATCH_MAX_CHARS`/`LLM_FALLBACK_PROVIDERS`) | Параметры батчинга и порядка провайдеров | Возможно — если решение «менять» |

## Тесты, кодирующие старое поведение

| Файл:строка | Как выражено старое поведение |
|---|---|

## Готово когда

- Явное решение: менять параметры / оставить как есть — с обоснованием на цифрах тикета 01.
- Если решение «менять» — заведён отдельный тикет-задача на реализацию (не в этом тикете).

## Answer

<Заполняется в конце сессии.>
