# <Заголовок: что не так, одной строкой>

Type: task | grilling | research | prototype
Скилл: `<имя>` — <одна причина> | не нужен — <почему>
Status: open
Blocked by: —
Ветка: `<префикс>/<slug>`
Код-тикет: — <только для grilling/research; заполняется ссылкой, когда `## Answer` заканчивается «код — отдельным тикетом» (R-10/R-11); для task/prototype, где код — часть этого же тикета, строка не нужна>

## Question

<Что именно не так и почему это стоит трогать. Без плана решения.>

## Контекст

- <Файлы с номерами строк, состояние на `master`, смежные тикеты.>

## Цифры

Числа приводятся только как снимок с датой — «на ГГГГ-ММ-ДД — N». Рядом обязателен запрос, которым число получено, и пометка, прогнан ли он.

```sql
-- SQL: не прогнан
```

`SQL: прогнан <дата>` ставит сессия, которая его реально выполнила. Непрогнанный запрос — гипотеза о схеме, а не факт: в тикете 01 такой запрос ссылался на колонку, которой нет в CTE.

`Код-тикет:` в шапке остаётся `—`, пока код не нужен или ещё не заведён; при закрытии карты (шаг 8, R-10) пустая строка на `grilling`/`research`-тикете с «код — отдельным тикетом» в `## Answer` — сигнал, что обещание повисло: в p2-data-fixes так повисло шесть решений (INC-15).

## Кто зависит

| Место | Что делает | Меняется |
|---|---|---|

## Тесты, кодирующие старое поведение

| Файл:строка | Как выражено старое поведение |
|---|---|

Заполняется поиском **по значению**, а не по имени символа: тест может держать поведение литералом и не упоминать ни функцию, ни константу. Раздел остаётся гипотезой — сессия всё равно делает собственный греп (`CLAUDE.md`, «Локализация до первой правки»).

## Готово когда

- <Проверяемый критерий, а не намерение.>
- Выполнено обновление файла map.md

## Answer

<Заполняется в конце сессии.>

Локализация: `<команда>`, найдено N мест
Расхождения цифр: <или «нет»>
